// GENERATED by tool/generate_rpc.dart — do not edit.
// Regenerate after any DAO signature change; CI checks the diff is empty.

import 'package:core/database/dao/tv_show_dao.dart';
import 'package:core/models/data_source.dart';
import 'package:core/models/tv_episode.dart';
import 'package:core/models/tv_season.dart';
import 'package:core/models/tv_show.dart';
import 'package:core/rpc/rpc_codec.dart';

/// Server-side entry point: runs [method] on a real [TvShowDao].
Future<Object?> dispatchTvShowDao(
  TvShowDao dao,
  String method,
  Map<String, Object?> args,
) async {
  switch (method) {
    case 'clearEpisodesByShow':
      await dao.clearEpisodesByShow(
        decodeEnum<DataSource>(args['source'], DataSource.values),
        decodeInt(args['showId']),
      );
      return null;

    case 'clearTvSeasons':
      await dao.clearTvSeasons();
      return null;

    case 'clearTvShows':
      await dao.clearTvShows();
      return null;

    case 'getAllWatchedEpisodes':
      final List<Map<String, Object?>> value = await dao
          .getAllWatchedEpisodes();
      return value.map((Map<String, Object?> e) => encodeRow(e)).toList();

    case 'getEpisodesByShowAndSeason':
      final List<TvEpisode> value = await dao.getEpisodesByShowAndSeason(
        decodeEnum<DataSource>(args['source'], DataSource.values),
        decodeInt(args['showId']),
        decodeInt(args['seasonNumber']),
      );
      return value
          .map(
            (TvEpisode e) => <String, Object?>{
              'tmdbShowId': encodeInt(e.tmdbShowId),
              'seasonNumber': encodeInt(e.seasonNumber),
              'episodeNumber': encodeInt(e.episodeNumber),
              'name': e.name,
              'overview': e.overview,
              'airDate': e.airDate,
              'stillUrl': e.stillUrl,
              'runtime': encodeIntOrNull(e.runtime),
              'source': encodeEnum(e.source),
            },
          )
          .toList();

    case 'getEpisodesByShowId':
      final List<TvEpisode> value = await dao.getEpisodesByShowId(
        decodeEnum<DataSource>(args['source'], DataSource.values),
        decodeInt(args['showId']),
      );
      return value
          .map(
            (TvEpisode e) => <String, Object?>{
              'tmdbShowId': encodeInt(e.tmdbShowId),
              'seasonNumber': encodeInt(e.seasonNumber),
              'episodeNumber': encodeInt(e.episodeNumber),
              'name': e.name,
              'overview': e.overview,
              'airDate': e.airDate,
              'stillUrl': e.stillUrl,
              'runtime': encodeIntOrNull(e.runtime),
              'source': encodeEnum(e.source),
            },
          )
          .toList();

    case 'getTvSeasonsByShowId':
      final List<TvSeason> value = await dao.getTvSeasonsByShowId(
        decodeEnum<DataSource>(args['source'], DataSource.values),
        decodeInt(args['showId']),
      );
      return value
          .map(
            (TvSeason e) => <String, Object?>{
              'tmdbShowId': encodeInt(e.tmdbShowId),
              'seasonNumber': encodeInt(e.seasonNumber),
              'name': e.name,
              'episodeCount': encodeIntOrNull(e.episodeCount),
              'posterUrl': e.posterUrl,
              'airDate': e.airDate,
              'source': encodeEnum(e.source),
            },
          )
          .toList();

    case 'getTvShowByTmdbId':
      final TvShow? value = await dao.getTvShowByTmdbId(
        decodeInt(args['tmdbId']),
        source: args.containsKey('source')
            ? decodeEnum<DataSource>(args['source'], DataSource.values)
            : DataSource.tmdb,
      );
      return encodeNullable<TvShow>(
        value,
        (TvShow v) => <String, Object?>{
          'tmdbId': encodeInt(v.tmdbId),
          'title': v.title,
          'originalTitle': v.originalTitle,
          'posterUrl': v.posterUrl,
          'backdropUrl': v.backdropUrl,
          'overview': v.overview,
          'genres': v.genres?.map((String e) => e).toList(),
          'firstAirYear': encodeIntOrNull(v.firstAirYear),
          'totalSeasons': encodeIntOrNull(v.totalSeasons),
          'totalEpisodes': encodeIntOrNull(v.totalEpisodes),
          'rating': v.rating,
          'status': v.status,
          'externalUrl': v.externalUrl,
          'cachedAt': encodeIntOrNull(v.cachedAt),
          'source': encodeEnum(v.source),
        },
      );

    case 'getTvShowsByTmdbIds':
      final List<TvShow> value = await dao.getTvShowsByTmdbIds(
        asList(args['tmdbIds']).map((Object? e) => decodeInt(e)).toList(),
      );
      return value
          .map(
            (TvShow e) => <String, Object?>{
              'tmdbId': encodeInt(e.tmdbId),
              'title': e.title,
              'originalTitle': e.originalTitle,
              'posterUrl': e.posterUrl,
              'backdropUrl': e.backdropUrl,
              'overview': e.overview,
              'genres': e.genres?.map((String e) => e).toList(),
              'firstAirYear': encodeIntOrNull(e.firstAirYear),
              'totalSeasons': encodeIntOrNull(e.totalSeasons),
              'totalEpisodes': encodeIntOrNull(e.totalEpisodes),
              'rating': e.rating,
              'status': e.status,
              'externalUrl': e.externalUrl,
              'cachedAt': encodeIntOrNull(e.cachedAt),
              'source': encodeEnum(e.source),
            },
          )
          .toList();

    case 'getWatchedEpisodeCount':
      final int value = await dao.getWatchedEpisodeCount(
        decodeInt(args['collectionId']),
        decodeEnum<DataSource>(args['source'], DataSource.values),
        decodeInt(args['showId']),
      );
      return encodeInt(value);

    case 'getWatchedEpisodes':
      final Map<(int, int), DateTime?> value = await dao.getWatchedEpisodes(
        decodeInt(args['collectionId']),
        decodeEnum<DataSource>(args['source'], DataSource.values),
        decodeInt(args['showId']),
      );
      return value.entries
          .map(
            (MapEntry<(int, int), DateTime?> e) => <String, Object?>{
              'k': <String, Object?>{
                r'$1': encodeInt(e.key.$1),
                r'$2': encodeInt(e.key.$2),
              },
              'v': encodeDateTimeOrNull(e.value),
            },
          )
          .toList();

    case 'getWatchedEpisodesForShow':
      final Set<(int, int)> value = await dao.getWatchedEpisodesForShow(
        decodeEnum<DataSource>(args['source'], DataSource.values),
        decodeInt(args['showId']),
      );
      return value
          .map(
            ((int, int) e) => <String, Object?>{
              r'$1': encodeInt(e.$1),
              r'$2': encodeInt(e.$2),
            },
          )
          .toList();

    case 'markEpisodeUnwatched':
      await dao.markEpisodeUnwatched(
        decodeInt(args['collectionId']),
        decodeEnum<DataSource>(args['source'], DataSource.values),
        decodeInt(args['showId']),
        decodeInt(args['seasonNumber']),
        decodeInt(args['episodeNumber']),
      );
      return null;

    case 'markEpisodeWatched':
      await dao.markEpisodeWatched(
        decodeInt(args['collectionId']),
        decodeEnum<DataSource>(args['source'], DataSource.values),
        decodeInt(args['showId']),
        decodeInt(args['seasonNumber']),
        decodeInt(args['episodeNumber']),
      );
      return null;

    case 'markEpisodeWatchedAt':
      await dao.markEpisodeWatchedAt(
        decodeInt(args['collectionId']),
        decodeEnum<DataSource>(args['source'], DataSource.values),
        decodeInt(args['showId']),
        decodeInt(args['seasonNumber']),
        decodeInt(args['episodeNumber']),
        decodeIntOrNull(args['watchedAtMs']),
      );
      return null;

    case 'markEpisodesWatchedAt':
      await dao.markEpisodesWatchedAt(
        decodeInt(args['collectionId']),
        decodeEnum<DataSource>(args['source'], DataSource.values),
        decodeInt(args['showId']),
        asList(args['episodes'])
            .map(
              (Object? e) => (
                decodeInt(asObject(e)[r'$1']),
                decodeInt(asObject(e)[r'$2']),
                decodeIntOrNull(asObject(e)[r'$3']),
              ),
            )
            .toList(),
      );
      return null;

    case 'markSeasonWatched':
      await dao.markSeasonWatched(
        decodeInt(args['collectionId']),
        decodeEnum<DataSource>(args['source'], DataSource.values),
        decodeInt(args['showId']),
        decodeInt(args['seasonNumber']),
        asList(
          args['episodeNumbers'],
        ).map((Object? e) => decodeInt(e)).toList(),
      );
      return null;

    case 'unmarkSeasonWatched':
      await dao.unmarkSeasonWatched(
        decodeInt(args['collectionId']),
        decodeEnum<DataSource>(args['source'], DataSource.values),
        decodeInt(args['showId']),
        decodeInt(args['seasonNumber']),
      );
      return null;

    case 'upsertEpisodes':
      await dao.upsertEpisodes(
        asList(args['episodes'])
            .map(
              (Object? e) => TvEpisode(
                tmdbShowId: decodeInt(asObject(e)['tmdbShowId']),
                seasonNumber: decodeInt(asObject(e)['seasonNumber']),
                episodeNumber: decodeInt(asObject(e)['episodeNumber']),
                name: asObject(e)['name'] as String,
                overview: asObject(e)['overview'] as String?,
                airDate: asObject(e)['airDate'] as String?,
                stillUrl: asObject(e)['stillUrl'] as String?,
                runtime: decodeIntOrNull(asObject(e)['runtime']),
                source: decodeEnum<DataSource>(
                  asObject(e)['source'],
                  DataSource.values,
                ),
              ),
            )
            .toList(),
      );
      return null;

    case 'upsertTvSeasons':
      await dao.upsertTvSeasons(
        asList(args['seasons'])
            .map(
              (Object? e) => TvSeason(
                tmdbShowId: decodeInt(asObject(e)['tmdbShowId']),
                seasonNumber: decodeInt(asObject(e)['seasonNumber']),
                name: asObject(e)['name'] as String?,
                episodeCount: decodeIntOrNull(asObject(e)['episodeCount']),
                posterUrl: asObject(e)['posterUrl'] as String?,
                airDate: asObject(e)['airDate'] as String?,
                source: decodeEnum<DataSource>(
                  asObject(e)['source'],
                  DataSource.values,
                ),
              ),
            )
            .toList(),
      );
      return null;

    case 'upsertTvShow':
      await dao.upsertTvShow(
        TvShow(
          tmdbId: decodeInt(asObject(args['tvShow'])['tmdbId']),
          title: asObject(args['tvShow'])['title'] as String,
          originalTitle: asObject(args['tvShow'])['originalTitle'] as String?,
          posterUrl: asObject(args['tvShow'])['posterUrl'] as String?,
          backdropUrl: asObject(args['tvShow'])['backdropUrl'] as String?,
          overview: asObject(args['tvShow'])['overview'] as String?,
          genres: asObject(args['tvShow'])['genres'] == null
              ? null
              : asList(
                  asObject(args['tvShow'])['genres'],
                ).map((Object? e) => e as String).toList(),
          firstAirYear: decodeIntOrNull(
            asObject(args['tvShow'])['firstAirYear'],
          ),
          totalSeasons: decodeIntOrNull(
            asObject(args['tvShow'])['totalSeasons'],
          ),
          totalEpisodes: decodeIntOrNull(
            asObject(args['tvShow'])['totalEpisodes'],
          ),
          rating: (asObject(args['tvShow'])['rating'] as num?)?.toDouble(),
          status: asObject(args['tvShow'])['status'] as String?,
          externalUrl: asObject(args['tvShow'])['externalUrl'] as String?,
          cachedAt: decodeIntOrNull(asObject(args['tvShow'])['cachedAt']),
          source: decodeEnum<DataSource>(
            asObject(args['tvShow'])['source'],
            DataSource.values,
          ),
        ),
      );
      return null;

    case 'upsertTvShows':
      await dao.upsertTvShows(
        asList(args['tvShows'])
            .map(
              (Object? e) => TvShow(
                tmdbId: decodeInt(asObject(e)['tmdbId']),
                title: asObject(e)['title'] as String,
                originalTitle: asObject(e)['originalTitle'] as String?,
                posterUrl: asObject(e)['posterUrl'] as String?,
                backdropUrl: asObject(e)['backdropUrl'] as String?,
                overview: asObject(e)['overview'] as String?,
                genres: asObject(e)['genres'] == null
                    ? null
                    : asList(
                        asObject(e)['genres'],
                      ).map((Object? e) => e as String).toList(),
                firstAirYear: decodeIntOrNull(asObject(e)['firstAirYear']),
                totalSeasons: decodeIntOrNull(asObject(e)['totalSeasons']),
                totalEpisodes: decodeIntOrNull(asObject(e)['totalEpisodes']),
                rating: (asObject(e)['rating'] as num?)?.toDouble(),
                status: asObject(e)['status'] as String?,
                externalUrl: asObject(e)['externalUrl'] as String?,
                cachedAt: decodeIntOrNull(asObject(e)['cachedAt']),
                source: decodeEnum<DataSource>(
                  asObject(e)['source'],
                  DataSource.values,
                ),
              ),
            )
            .toList(),
      );
      return null;

    default:
      throw RpcCodecException('TvShowDao has no method "$method"');
  }
}
