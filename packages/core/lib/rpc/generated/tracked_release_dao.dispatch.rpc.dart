// GENERATED by tool/generate_rpc.dart — do not edit.
// Regenerate after any DAO signature change; CI checks the diff is empty.

import 'package:core/database/dao/tracked_release_dao.dart';
import 'package:core/models/data_source.dart';
import 'package:core/models/media_type.dart';
import 'package:core/models/tracked_release.dart';
import 'package:core/rpc/rpc_codec.dart';

/// Server-side entry point: runs [method] on a real [TrackedReleaseDao].
Future<Object?> dispatchTrackedReleaseDao(
  TrackedReleaseDao dao,
  String method,
  Map<String, Object?> args,
) async {
  switch (method) {
    case 'deleteOrphaned':
      await dao.deleteOrphaned();
      return null;

    case 'getAll':
      final List<TrackedRelease> value = await dao.getAll();
      return value
          .map(
            (TrackedRelease e) => <String, Object?>{
              'externalId': encodeInt(e.externalId),
              'source': encodeEnum(e.source),
              'mediaType': encodeEnum(e.mediaType),
              'createdAt': encodeDateTime(e.createdAt),
            },
          )
          .toList();

    case 'getTrackedKeys':
      final Set<(int, String, String)> value = await dao.getTrackedKeys();
      return value
          .map(
            ((int, String, String) e) => <String, Object?>{
              r'$1': encodeInt(e.$1),
              r'$2': e.$2,
              r'$3': e.$3,
            },
          )
          .toList();

    case 'isTracked':
      final bool value = await dao.isTracked(
        decodeInt(args['externalId']),
        decodeEnum<DataSource>(args['source'], DataSource.values),
        decodeEnum<MediaType>(args['mediaType'], MediaType.values),
      );
      return value;

    case 'subscribe':
      await dao.subscribe(
        decodeInt(args['externalId']),
        decodeEnum<DataSource>(args['source'], DataSource.values),
        decodeEnum<MediaType>(args['mediaType'], MediaType.values),
      );
      return null;

    case 'unsubscribe':
      await dao.unsubscribe(
        decodeInt(args['externalId']),
        decodeEnum<DataSource>(args['source'], DataSource.values),
        decodeEnum<MediaType>(args['mediaType'], MediaType.values),
      );
      return null;

    default:
      throw RpcCodecException('TrackedReleaseDao has no method "$method"');
  }
}
